// BambooHR time-off data is not yet integrated — this renders nothing until
// the TimeOff model exists and who's_out is synced on each page load.
export default function OutToday() {
  return null
}
